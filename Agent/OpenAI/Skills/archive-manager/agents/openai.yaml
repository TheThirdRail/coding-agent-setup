interface:
  display_name: "Archive Manager"
  short_description: "Route code, docs, git, graph, and memory archive retrieval or indexing through one on-demand context skill."
  default_prompt: "Use $archive-manager to route this request to the correct on-demand lane."
