interface:
  display_name: "Agent Builder"
  short_description: "Create or maintain agent capabilities, skills, workflows, rules, automations, and custom MCP tools."
  default_prompt: "Use $agent-builder to route this request to the correct on-demand lane."
